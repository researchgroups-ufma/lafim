# Dependências — geradas pelo npm install, não precisam ir para o repositório
node_modules/

# Build do Next.js — gerado pelo npm run build
.next/
out/

# Variáveis de ambiente — NUNCA commitar, contém dados sensíveis
.env
.env.local
.env.production

# Sistema operacional
.DS_Store
Thumbs.db

# Editor
.vscode/settings.json
CLAUDE.md 
prompt.md
DESIGN.md

